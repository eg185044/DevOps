# Copy to group_vars/secrets-vault.yml, edit values, then encrypt:
# ansible-vault encrypt group_vars/secrets-vault.yml
vault_db_password: "CHANGE_ME_STRONG_PASSWORD_123!"
vault_flask_secret_key: "CHANGE_ME_APP_SECRET_123!"
