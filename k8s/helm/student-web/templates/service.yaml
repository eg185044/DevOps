apiVersion: v1
kind: Service
metadata:
  name: {{ include "student-web.fullname" . }}
  labels:
    {{- include "student-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "student-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
