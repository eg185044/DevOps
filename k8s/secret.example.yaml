# EXAMPLE ONLY - copy to secret.yaml, fill in real values, and NEVER commit secret.yaml.
# `secret.yaml` is already covered by .gitignore (see k8s/README.md "Secrets" section
# for the exact `kubectl create secret` command used instead of applying this file).
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: devops-app
  labels:
    app.kubernetes.io/part-of: erez-cv-platform
type: Opaque
stringData:
  DB_HOST: "REPLACE_ME.xxxxxxxxxx.eu-west-1.rds.amazonaws.com"
  DB_USERNAME: "erezadmin"
  DB_PASSWORD: "REPLACE_ME"
